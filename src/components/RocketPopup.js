import React from 'react';

const RocketPopup = ({ rocket, onClosePopup }) => {
  return (
    <div className="rocket-popup">
      {/* Display detailed rocket information */}
    </div>
  );
};

export default RocketPopup;
